npm commands:

npx create-react-app <appname>
//create react app

npm start
//start app at localhost:3000

npm run build
//build a build directory for deployment

npm run test
//execute the react test files

render() -> lifecycle method that render the output

jsx -> there must one parent element (div, Fragment, <>)

jsx -> React.createElement('div', {className: 'App'}, React.createElement('h1',ull, 'Hello react'))

this is equivalent to 
<div className = 'App>
    <h1>Hello react</h1>
</div>
